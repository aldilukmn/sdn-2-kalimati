Jika pengguna meminta untuk push atau commit kode, selalu gunakan format Conventional Commits (sesuaikan dengan 'git log -5'), kemudian eksekusi secara berurutan: git add ., git commit, dan git push.
